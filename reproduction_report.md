# Mini-quenching 复现报告

固定版本：Luberto & Furlanetto, arXiv:2609.19265v1；执行日期 2026-09-20。

## 结论与验收边界

独立项目已经实际安装、运行、调试和测试。**完整论文复现尚未通过验收。** 不能把本轮数值测试通过、或扫描程序运行结束，等同于作者物理实现已经恢复。

|论文图|状态|实际交付与限制|
|---|---|---|
|1|reproduced（参数化 IMF 校验）|式 (7) 六条曲线已恢复；最大相对差异 8.55×10⁻⁶。黑色经验 Chabrier 比较线未重新拟合，不计入复现范围。|
|2|blocked（完整反馈）；approximate（SN-only）|两个质量均运行。含全部反馈项的 10⁹ Msun 轨道无法稳定启动；SN-only 两轨道完成，但淬火时长不匹配原图。|
|3|not_run|核心完整反馈未通过，未输出冒充原文的反馈占比图。|
|4|approximate|解析式、简化数值、固定/增长暗晕以及两种 SFH/壳质量约定已计算，仍有定量差异。|
|5|blocked（完整反馈）；approximate（SN-only）|效率、IMF、总恒星质量映射扫描均执行；失败点和未返回下限明确保留。|
|6|not_run|按先通过核心的顺序，未继续声称观测形成率边界复现。|
|7|not_run|核心尚未通过，未提交额外红移动力学结果。|
|8|blocked（完整反馈）；approximate（SN-only）|覆盖率和低初始气体对照均运行；总气体吸积率保持不变。|
|9|not_run|未进入 HMF/淬火比例阶段，未安装 Colossus。|
|10|not_run|Compton 冷却方程通过独立测试，但没有将单元测试称为该图复现。|

## 文献与外部输入

已下载指定 v1 的 HTML、PDF、TeX 源包并联合核查。原始 PDF 38 页，源包含所有图的独立矢量 PDF。原文链接和源包未发现作者模型代码、参数文件或机器可读求解器曲线。

参考数据由这些原始矢量图的路径坐标提取：图 1/2/4/5/8 共 31 条参考曲线。记录 PDF 路径编号、坐标变换、坐标精度和显示裁剪；它们是**原图采样**，不是作者求解器数据。采用约 2 Myr、0.03 dex 的保守读图尺度，图 2 半径比约 0.1；原图 175/75 Myr 的括号本身是粗略标注。没有用这些数据调整参数。

Lyα 原始资料使用 [Nebrin 等人公开实现](https://github.com/olofnebrin/Lyman-alpha-feedback)，commit `e89bf76c43006177d3759b213b088536aa1c7b05`，保留 MIT 许可；另取得 arXiv:2409.19288 源包。冷却函数来自 Schure et al. 2009（arXiv:0909.5204）TeX 表 2，共 110 行；使用已经包含 ne/nH 的 Λhd，避免重复计电子密度。原始文件版本/校验值见 `data/manifest.json`。

## 物理定义核查

1. **等温球而非点质量**：联用式 (1)、(2)、(22) 得 g=2GMh/(rvir r)。这意味着该参数化在 rvir 包围 2Mh，与通常“总质量”解释有因子 2 冲突。保留原文因子 2，另测因子 1 分支，不声称冲突已经消除。
2. **IMF**：式 (7) 是 dN/dm，单位形成质量归一化，0.08–100 Msun；正文 α=2.35、mchar=.2、β=1.6 定义 Chabrier-like。图 1 图注 α=1.35、图 5 图注 α=−2.35 的冲突明确记录；矢量曲线校验支持正文解释。
3. **效率与质量**：fstar 控制 SFR/Mgas/tff；ftilde=.01–.1 只用于观测横坐标 Mstar,total=ftilde fb Mh。单次形成质量另存，未拿它代替观测总恒星质量。
4. **固定红移与增长**：单周期 z 固定，Mh 指数增长；R0(t)=κrvir(t) 连续更新。没有借用 AuroraLF 参数。
5. **覆盖率**：mseg 初始为 fcover Mg；反馈力乘 fcover，吸积质量率仍是全部 fb Mdot_h。代码测试检查覆盖变化下吸积动量不被同乘缩小。
6. **回落**：只在向外时加载的字面分支和返回前持续加载分支均保存。CLI 全扫描是后者；它是可审计对照，不是确认过的作者实现。
7. **完整反馈缺失约定**：原文没有给出 R*(m)、冷却壳层厚度/密度、离子态和 MF 的详细调用参数。full_approx 采用 R*=Rsun m^0.5、厚度/r=.1、X=.753、中性壳层、中心静态源及最低稳定热平衡；均属显式推断。厚度 .01/.3、速度项、半径指数 .7 等有限分支已测，不能据此证明其他闭合无影响。

## 实现与调试

实现包含气体储库解析演化、IMF/寿命、SN/UV/Lyα 年龄响应、Nebrin 力倍增、Schure 冷却平衡、热泡压力及 Compton/PdV、等温球引力、覆盖部分的质量/动量、移动边界启动与返回事件、解析质量上限和参数扫描。

年龄核预计算并缓存。形成期 SFH 是两个指数项的解析组合，对核预积分后，在因果区间取积分差；RHS 不追加 SFH。最早/最晚 SN 年龄由 100/8 Msun 的寿命确定（0.1/55.2427 Myr）。关停之后保留老恒星辐射和延迟 SN。

首次实现曾在非网格关停时刻出现反馈插值不连续；已修复，并加入关停连续性回归测试。可解析启动之后，在相对半径最大值处再启用返回检测；转向不是淬火结束。回落压缩项仍按原式 −5Pv/r 计算，因此一些轨道会反弹多次。没有将首次转向或反弹前的时间冒充 tquench。

含辐射配置暴露出另一种问题：推出条件首次成立时关停 SFR，会使辐射力立即衰减；某些配置随后不能解析出正的壳层分离。小于积分误差尺度的擦边事件以及无法分离的轨道归为 numerical_failure，不能填成零时长“成功”。另对速度切换导致的异常积分设置 50000 次 RHS 计算上限，超过即明确报错，不返回替代数据。

## 实际结果

含全部反馈项的 full 扫描共 467 条配置：26 条 completed_cycle、156 条 not_launched_within_window、285 条 numerical_failure。失败中 220 条未能离开移动边界，65 条分离幅度不足以解析。**大量失败本身说明 full_approx 不是通过验收的论文全模型。**

SN-only full 扫描共 499 条（含阈值加密）：276 条 completed_cycle、177 条窗口内未启动、41 条 numerical_failure、5 条到 700 Myr 仍未返回。最后 5 条只给 tquench 下限；不会称作永久逃逸。

|图 2 配置（until_return）|tSF [Myr]|tquench [Myr]|最大 r/R0|原图粗读 tquench|
|---|---:|---:|---:|---:|
|10⁸ Msun，full_approx|5.59984|297.4648|5.63324|约 175|
|10⁹ Msun，full_approx|—|—|—|约 75；本实现启动失败|
|10⁸ Msun，sn_only|5.96176|315.0674|6.42736|约 175|
|10⁹ Msun，sn_only|16.00881|182.8604|2.07325|约 75|

失败行不是没有执行，也不是 tSF=0；错误原因和配置存在 JSON 中。两条 SN-only 轨道的所有要求诊断量已输出到 CSV；全反馈 10⁹ Msun 没有伪造完整时间序列。

图 4 与动态轨道分开。`numerical_mass` 使用数值储库 SFH/消耗后的壳质量；其与原图的最大相对差异在 z=3/6/9 约 15%/41%/84%。按第 3 节近似另算固定初始 SFR 和 Msh=fb Mh 的分支，最大差异降到约 2%/10%/23%。这是原文可支持的有限约定比较，不是参数拟合；两分支均保留，作者精确数值约定仍未确定。全部残差见 `data/figure4_conventions.json` 和 `paper_comparison.json`。

图 5/8 的残差只在已完成且参考数据覆盖的点上计算；失败/未返回不混进相对误差平均值。原图的观测星形点未叠加，因为没有重新取得它们的可追溯数值表。输出的观测质量映射范围不是重新推导的观测数据。

## 测试、收敛与耗时

实际 pytest：24 项通过。包括多 IMF 质量积分、SN 核计数、瞬时/恒定/关停历史反馈、非网格关停连续性、Pr⁵ 不变量、固定半径 Compton 指数衰减、气体质量守恒、覆盖率与动量恒等式、无反馈不启动、返回及未返回、不可解析启动拒绝、缓存配置隔离。Ruff 的选定错误检查和格式检查另外记录。

收敛单独缩小 history_dt、收紧 rtol/atol、减小 max_step，并有组合精细参考。10⁸ Msun full_approx 和 10⁹ Msun sn_only（均 until_return）三项指标相对差异都小于约 0.02%，达到 1% 工程目标。**该结论不覆盖 full_approx 的失败点。** SN-only fstar=.001 的临界质量在三个历史格距下均落于约 [2.8741,2.8822]×10¹⁰ Msun，区间宽约 0.28%。原图误差则远大于这些数值误差，不能归因于简单积分精度不足。

|实测任务|耗时|说明|
|---|---:|---|
|首轮可完成单轨道|约 0.7–1.1 s|CPU；不同物理配置不同|
|135 点小扫描|14.4 s|3 个工作进程；随后才执行 full|
|full_approx 全扫描|41.9 s|467 个配置记录，含失败|
|sn_only 全扫描|71.2 s|499 个配置记录，含 32 个加密点|
|最终 validate|约 12.5 s|利用已存在的部分指纹缓存；日志给出逐项原始计时|
|pytest|约 4 s|精确时间见 outputs/logs/pytest.txt|

断点复用只发生在配置和代码指纹一致时；缓存命中与原始轨道耗时分别记录。首次粗扫描据实测预计 full 为分钟量级，实际结果与此相符。没有 GPU/集群/付费资源，没有大快照或嵌套并行。

## 剩余差异与后续所需证据

最需要的是作者对壳层热学闭合、R*(m)、形成区释放条件、回落时质量加载、以及图 4 数值 SFH 的明确说明或原始代码。这些细节影响首次启动和回落，而不是简单换小步长能消除。当前项目已经完成不依赖这些信息的 IMF 校验、解析/数值近似、单元测试、扫描及失败定位；尚不能替作者未说明的约定作出唯一判决。

审核资料：`slides/reproduction_review.pdf`；原始/重画图、机器可读数据、配置、源文件和运行日志均留在本项目中。未修改 AuroraLF 物理实现，初次交付未提交或推送；后续按用户明确要求发布独立审阅仓库，物理结果不因此改变。
